name: C CI for Xenly programming language (bash script)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: bash installing test
      run: sh main.sh
    - name: xenly test
      run: time ./xenly examples/hello.xe
    - name: xenlyc test
      run: time ./xenlyc examples/hello.xe -o hello
    - name: hello test
      run: time ./hello
